# Maintainer: Krelpin Developers <dev@krelpin.org>

pkgname=qt6-shadertools
_qtver=6.8.2
pkgver=${_qtver/-/}
pkgrel=1
pkgdesc='Tools for the cross-platform Qt shader pipeline'
arch=('aarch64' 'x86_64' 'armv7h' 'riscv64')
url='https://www.qt.io'
license=('GPL-3.0-only' 'LGPL-3.0-only' 'LicenseRef-Qt-Commercial')
depends=('qt6-base')
makedepends=('cmake' 'ninja' 'python' 'perl')
groups=('qt6')
_pkgfn="qtshadertools-everywhere-src-${_qtver}"
source=("https://download.qt.io/official_releases/qt/${_qtver%.*}/${_qtver}/submodules/${_pkgfn}.tar.xz")
sha256sums=('SKIP')

build() {
  cmake -B build -S "${_pkgfn}" -G Ninja \
    -DCMAKE_INSTALL_PREFIX=/usr \
    -DCMAKE_BUILD_TYPE=Release \
    -DINSTALL_BINDIR=lib/qt6/bin \
    -DINSTALL_PUBLICBINDIR=bin \
    -DINSTALL_LIBEXECDIR=lib/qt6 \
    -DINSTALL_DOCDIR=share/doc/qt6 \
    -DINSTALL_ARCHDATADIR=lib/qt6 \
    -DINSTALL_DATADIR=share/qt6 \
    -DINSTALL_INCLUDEDIR=include/qt6 \
    -DINSTALL_MKSPECSDIR=lib/qt6/mkspecs \
    -DINSTALL_EXAMPLESDIR=share/doc/qt6/examples \
    -DCMAKE_MESSAGE_LOG_LEVEL=STATUS
  cmake --build build
}

package() {
  DESTDIR="${pkgdir}" cmake --install build
}
